import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  showLocation(address: string): void {
    // Logic to display address on map using external map service
  }
}